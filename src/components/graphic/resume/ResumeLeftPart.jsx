import React from 'react';
import classes from "../resume/Resume.module.css";

const ResumeLeftPart = (props) => {
    return (
        <div className={classes.resumeColumnLeft} {...props}>
            
        </div>
    );
};

export default ResumeLeftPart;