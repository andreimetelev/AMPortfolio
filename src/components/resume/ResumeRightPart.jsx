import React from 'react';
import classes from "../resume/Resume.module.css";

const ResumeRightPart = (props) => {
    return (
        <div className={classes.resumeColumnRight} {...props}>
            
        </div>
    );
};

export default ResumeRightPart;